// react-slick no trae sus propios tipos y no hay @types/react-slick
// publicado para esta versión; lo tratamos como `any` en vez de forzar un
// `// @ts-ignore` en cada import.
declare module "react-slick";
