# Archivo de ejemplo para variables de entorno del servidor
# Copia este archivo a .env y rellena los valores correspondientes.

# Contraseña para el administrador por defecto que se crea si la base de datos está vacía.
# Si se deja vacía, se genera una contraseña aleatoria y se imprime una sola vez en consola.
DEFAULT_ADMIN_PASSWORD=

# Secreto para firmar los JSON Web Tokens (JWT). Genera uno propio, por ejemplo:
# node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET=

# Envío de correo vía la API HTTP de SendGrid (no SMTP): Railway bloquea el
# SMTP saliente (puertos 25/465/587) en los planes Trial/Hobby, solo lo
# habilita desde el plan Pro. SENDGRID_API_KEY se genera en
# app.sendgrid.com → Settings → API Keys (permiso "Mail Send" alcanza).
SENDGRID_API_KEY=

# Dirección remitente. Debe estar verificada en SendGrid como "Single Sender"
# (Settings → Sender Authentication → Single Sender Verification) — sin eso
# SendGrid rechaza el envío. Sin dominio propio autenticado, el single sender
# es solo para pruebas (peor entregabilidad); para producción real conviene
# comprar un dominio y pasar a autenticación de dominio en SendGrid.
EMAIL_USER=

# URL del frontend: se usa para restringir CORS y para los enlaces que resuelve el frontend
# (p. ej. reset de contraseña).
FRONTEND_URL=http://localhost:3000

# URL pública de este mismo backend: se usa para los enlaces que el backend resuelve
# directamente (p. ej. verificación de cuenta, GET /api/auth/verificar/:token).
# En Railway normalmente no hace falta fijarla a mano: si el servicio tiene dominio
# público, Railway inyecta RAILWAY_PUBLIC_DOMAIN y se usa automáticamente.
API_URL=

# Ruta del volumen persistente en Railway. Cuando está definida, tanto la base
# de datos SQLite (server/src/db/client.js) como las imágenes subidas
# (server/src/middlewares/upload.js) se guardan ahí en vez de en el disco
# efímero del contenedor. Railway la inyecta automáticamente SOLO si el
# servicio tiene un volumen montado (Settings → Volumes).
#
# ADVERTENCIA: sin un volumen persistente configurado en Railway, esta
# variable queda vacía y la base de datos y los uploads se guardan en el
# filesystem efímero del contenedor — se pierden por completo en cada
# redeploy (incluyendo deploys automáticos por push a la rama conectada).
RAILWAY_VOLUME_MOUNT_PATH=
